<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles/build.css" />
  </head>
  <body>
    <%- include('navbar') %>
    <h1 class="text-3xl font-bold text-gray-800 mb-4 border-2"><%= title %></h1>
    <ul class="flex">
      <% items.forEach( (item) => { %> <%- include('items', { item_name:
      item.item_name }) %> <% }); %>
    </ul>
  </body>
</html>
